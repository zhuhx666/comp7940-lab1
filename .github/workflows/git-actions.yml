name: Python safety check
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Python Safety Check
        uses: aufdenpunkt/python-safety-check@v1.0.4
